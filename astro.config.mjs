// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://guanfuge.github.io',
  base: '/guanfu-astro/',
  output: 'static',
});
